### 1. Why do we use Object-Oriented Programming instead of scattering related data and functions across many separate variables? What is the main benefit of grouping them together?

### 2. How does OOP treat **state** (data that changes over time) differently from Functional Programming? 

### 3. What is **encapsulation**, and why is it useful to hide internal details of an object and expose only specific ways to interact with it?

### 4. What is the difference between a **class**, an **object** (instance), and a **method**? How do they work together?
